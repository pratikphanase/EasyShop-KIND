apiVersion: v1
kind: Secret

metadata:
  name: easyshop-secret

type: Opaque

stringData:

  NEXTAUTH_SECRET: {{ .Values.secret.NEXTAUTH_SECRET | quote }}

  JWT_SECRET: {{ .Values.secret.JWT_SECRET | quote }}
